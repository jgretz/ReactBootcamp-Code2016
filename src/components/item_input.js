import React, { Component } from 'react';

export default class ItemInput extends Component {
  render() {
    return (
      <div>
        <input />
        <button>Add</button>
      </div>
    );
  }
}
